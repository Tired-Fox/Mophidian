<ul @if="not blank(nav)">
    <span>{nav.name}</span>
    <li @for="item in nav">
        <a @if="isinstance(item, mophidian.Page)" :href="item.url">{item.title}</a>
        <nav @elif="isinstance(item, mophidian.Nav)" :nav="item" />
        <span @else>Unkown nav item</span>
    </li>
</ul>